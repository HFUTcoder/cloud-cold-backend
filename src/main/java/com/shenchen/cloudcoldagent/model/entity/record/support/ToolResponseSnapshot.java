package com.shenchen.cloudcoldagent.model.entity.record.support;

/**
 * 创建 `ToolResponseSnapshot` 实例。
 *
 * @param id id 参数。
 * @param name name 参数。
 * @param responseData responseData 参数。
 */
/**
 * `ToolResponseSnapshot` 记录对象。
 */
public record ToolResponseSnapshot(String id, String name, String responseData) {
}
