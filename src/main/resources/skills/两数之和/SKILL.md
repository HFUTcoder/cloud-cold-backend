---
name: 两数之和
description: 用于接收两个数字并计算它们的和，适合简单加法计算场景
---

# 两数之和

## ScriptDefinitions

### Script 1

- path: `scripts/add_numbers.py`
- purpose: 计算两个数字的和

#### arguments

- `a` | number | required | 第一个数字
- `b` | number | required | 第二个数字

#### execution_rules

- 必须使用上述 `path`
- 不要猜测其他脚本名
- 只允许使用 `a` 和 `b` 作为参数名
- 如果用户已经明确给出两个数字，应直接组装为结构化参数并执行脚本

## 适用场景

当用户明确提出下面这类需求时，应优先使用本 skill：

- 计算两个数字的和
- 把两个整数或小数相加
- 已经给出 `a` 和 `b`，希望直接得到结果

## 使用规则

1. 如果当前问题只是普通问答，且没有明确要做“两数相加”，不要强行使用本 skill。
2. 一旦确认当前问题是在计算两个数字的和，必须优先通过本 skill 的工具链完成，不要直接心算作答。
3. 如果需要确认本 skill 下有哪些可执行脚本，先调用 `list_skill_resources`。
4. 进行加法计算时，优先执行脚本 `scripts/add_numbers.py`。
5. 执行脚本时，使用结构化参数对象传入两个参数：
   - `a`：第一个数字
   - `b`：第二个数字
6. 如果用户提供的是可直接解析的数字，直接调用脚本即可；不要把脚本源码原样输出给用户。
7. 不要直接根据模型常识给出加法结果，应以脚本返回结果为准。

## 推荐执行方式

推荐按如下步骤处理：

1. 判断用户是否真的在请求计算两数之和。
2. 如果脚本路径尚不明确，先调用 `list_skill_resources`。
3. 必要时读取参考资料：`references/两数之和-reference.md`
4. 执行脚本：`scripts/add_numbers.py`
5. 将脚本返回结果组织成简洁自然的最终回答
