---
name: 计算个人所得税
description: 用于根据月度收入、五险一金和专项附加扣除等信息估算个人所得税
---

# 计算个人所得税

## 【最高优先级全局约束】
1.  本Skill所有参数**唯一合法取值来源**：仅为用户本轮提问原文，或本Skill明确定义的默认值
2.  绝对禁止自主编造、脑补、填充任何参数数值，禁止使用任何测试值、示例值执行脚本
3.  执行脚本前必须完成参数校验，参数与用户提问不一致禁止执行
4.  最终回答必须100%基于本次脚本执行结果，禁止拼接矛盾的数值与内容

## ScriptDefinitions

### Script 1

- path: `scripts/calculate_individual_income_tax.py`
- purpose: 根据月度税前工资、五险一金和专项附加扣除计算个人所得税
- 【强制规则】本Skill唯一优先执行脚本，禁止使用任何其他脚本文件，禁止猜测修改脚本路径

#### arguments

- `salary` | number | required | 月度税前工资
   - 【强制规则】用户表述的「收入、月薪、工资、税前收入」必须100%精准映射为本字段
   - 【强制规则】本字段数值必须完全来自用户本轮提问原文，禁止修改、替换数值
- `insurance` | number | optional | default=0 | 月度五险一金个人缴纳部分
   - 【强制规则】用户未明确提及，必须固定传入默认值0，禁止编造任何非0数值
- `special_deduction` | number | optional | default=0 | 月度专项附加扣除
   - 【强制规则】用户未明确提及，必须固定传入默认值0，禁止编造任何非0数值
- `other_deduction` | number | optional | default=0 | 其他依法扣除项
   - 【强制规则】用户未明确提及，必须固定传入默认值0，禁止编造任何非0数值

#### execution_rules

- 必须使用上述固定`path`，禁止使用其他脚本
- 用户未提供的`insurance`、`special_deduction`、`other_deduction`，**必须**按默认值`0`执行
- 最终回答必须以脚本执行结果为准，禁止凭模型常识估算作答
- 执行脚本前必须校验：`salary`数值与用户提问原文完全一致，否则禁止执行

## 适用场景

当用户明确提出下面这类需求时，应优先使用本 skill：

- 计算个人所得税
- 估算月度应纳税额或税后收入
- 已经给出工资、五险一金、专项附加扣除等信息，希望得到个税结果

## 使用规则

1.  如果当前问题不是在请求计算个人所得税，不要强行使用本 skill。
2.  一旦确认当前问题是在计算个人所得税，**必须**优先通过本 skill 的工具链完成，禁止直接凭模型常识估算作答。
3.  进行个税计算时，**必须**优先执行脚本 `scripts/calculate_individual_income_tax.py`。
4.  执行脚本时，必须使用结构化参数对象传入完整参数，禁止省略、漏传参数。
5.  如果用户给的是可直接解析的数字，直接调用脚本即可；不要把脚本源码原样输出给用户。
6.  禁止向用户追问可选参数，只要用户给出了税前工资，即参数完整，可直接执行脚本。

## 推荐执行方式

推荐按如下步骤**严格按顺序执行**：

1.  判断用户是否真的在请求计算个人所得税，非对应场景不使用本Skill。
2.  从用户提问原文中，精准提取税前工资数字，赋值给`salary`参数。
3.  所有用户未明确提及的可选参数，全部赋值为固定默认值0。
4.  校验参数：`salary`与用户提问数值完全一致，其余可选参数符合默认值规则。
5.  执行固定脚本：`scripts/calculate_individual_income_tax.py`，传入校验后的完整参数。
6.  将脚本返回结果组织成简洁自然的最终回答。

## 【最终回答强制规范】
1.  最终回答的收入、个税等所有数值，必须100%来自用户提问和本次脚本执行结果
2.  必须明确告知用户：计算是否计入五险一金、专项附加扣除，未计入的需说明按默认值0核算
3.  禁止输出脚本源码、参数定义原文、工具调用过程给用户